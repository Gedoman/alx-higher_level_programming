#!/usr/bin/python3
"""module for is_kind_of_class mithod"""


def inherits_from(obj, a_class):
    """determins if an object is a subclass of a class"""
    return isinstance(obj, a_class) and isinstance(obj, a_class)
